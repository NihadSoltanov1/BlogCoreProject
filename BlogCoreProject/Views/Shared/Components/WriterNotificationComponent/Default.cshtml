@model List<EntityLayer.Concrete.Notification>

<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">
    <h6 class="p-3 mb-0">Bildirişlər</h6>

    @foreach (var i in Model)
    {
        <div class="dropdown-divider"></div>
        <a class="dropdown-item preview-item">
            <div class="preview-thumbnail">
                <div class="preview-icon @i.NotificationColor">
                    <i class="@i.NotificationIcon"></i>
                </div>
            </div>
            <div class="preview-item-content d-flex align-items-start flex-column justify-content-center">
                <h6 class="preview-subject font-weight-normal mb-1">@i.NotificationTitle</h6>
                <p class="text-gray ellipsis mb-0">@i.NotificationContent</p>
            </div>
        </a>

    }
    
   
    
    <div class="dropdown-divider"></div>
    <h6 class="p-3 mb-0 text-center">Bütün Bildirişlər</h6>
</div>
